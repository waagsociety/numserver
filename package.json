{
  "name": "numqueries",
  "version": "1.0.0",
  "description": "an open data proxy that delivers numerical values",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "= <=> (=)",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3",
    "request": "^2.66.0"
  }
}
